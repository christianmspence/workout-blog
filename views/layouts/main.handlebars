<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Fitter</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
        <link rel="stylesheet" href="/stylesheets/style.css">
    </head>

    <body>
        <div class="wrapper">
            <header class="wrapper d-flex justify-content-around align-items-center bg-danger bg-opacity-50 text-white" style="height: 100px;">
                <a href="/" class="page-title"><h1>Fitter</h1></a>
                <nav>
                    {{#if loggedIn}}
                    <a href="/profile" class="nav-link">Profile</a>
                    <button id="logout" class="nav-link">logout</button>
                    {{else}}
                    <a href="/login" class="nav-link">login/signup</a>
                    {{/if}}
                </nav>
            </header>

            <main class="container justify-content-around">
                {{{ body }}}
            </main>

            <footer class="wrapper bg-danger text-white bg-opacity-50 d-flex align-items-center">
                Contributors: Christian Spence, Miller Dicarlo, Jake Mendez
            </footer>
        </div>
        {{#if loggedIn}}
        <script src="/javascript/logout.js"></script>
        {{/if}}
    </body>
</html>